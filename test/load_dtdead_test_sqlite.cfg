# Configuration file for EventSetupTest_t

process TEST = {

	path p = { get, print }


	es_source = PoolDBESSource {  VPSet toGet = {
                                    {string record = "DTDeadFlagRcd" 
                                     string tag = "commissioning_deadlist"
                                     } }
		    		     bool loadAll = true
                                     string connect = "sqlite_file:commissioning_deadlist.db"
			             string timetype = "runnumber" 
                                     untracked string catalog = "file:commissioning_deadcatalog.xml"
                                     PSet DBParameters = {
                                       untracked string authenticationPath="."
                                       untracked int32 messageLevel=0
                                     }
				    }

	module print = AsciiOutputModule { }
	

#	source = EmptySource {untracked int32 maxEvents = 4 untracked uint32 firstRun = 1 untracked uint32 numberEventsInRun = 10}
        source = EmptySource {untracked uint32 firstRun = 0
                              untracked uint32 numberEventsInRun = 1}
        untracked PSet maxEvents = { untracked int32 input = 30 }

	module get = DTDeadPrint { }

}
